# API Profile Guard — perfection-loop round 2 handoff

## Outcome

Round 2 is complete and deployed at
<https://api-profile-guard.sociobot.in>. All findings from both adversarial
reviews are closed, including the three round-two findings. No acceptance gap is
known.

Implementation commit: `e9972f104343bf9fc0fb407564eecb5d43308eba`.
Deployment ID: `45b15c6e-1ada-44b3-a13e-262a0f641d30`.

## What changed

- The landing terminal now renders `site/demo-transcript.txt`, generated by
  running the current `apg demo`. A claim test compares every normalized line with
  a fresh run and requires distinct blocked and allowed fingerprints.
- “Production confirmation phrase” is now the only public term in landing copy,
  README, CLI help, and browser errors. A regression test rejects the old terms.
- The false “Node.js 20 or newer” README promise was removed. The package and
  lockfile enforce Vite's actual range: `>=20.19.0 <21 || >=22.12.0`.
- The one-click isolated demo, banner, reset, exit, demo storage namespace,
  first-screen wording, claims, real routes, metadata, focus, styled 404, legal
  links, mobile layout, offline shell, and pressroom visual identity were replayed
  and remain intact.
- `.factory/catalog-description.txt` is a 61-character verb-first line:
  “Block wrong-environment API requests before your client runs.”
- `site/live-check.mjs` now replays the cumulative route, metadata, 404, axe,
  first-screen, transcript, focus, reset, storage, touch-target, privacy,
  reduced-motion, and offline checks against production.

The complete finding-to-change-to-evidence map is in `.factory/polish-2.md`.

## How to run

```sh
npm ci
npm test
npm run lint
npm run build
npm run test:e2e
npm run verify:live -- .factory/polish-2-evidence
cargo build --release
cargo package --allow-dirty
```

Run a single claim with the exact command in `.factory/claims.json`. Regenerate
the checked terminal asset with `npm run demo:record`, then rerun
`@claim:cli-demo-sandbox` before committing it.

## Verification evidence

The remote clean clone was
`/tmp/api-profile-guard-polish2-clean.2jhNAD/repo` at
`e9972f104343bf9fc0fb407564eecb5d43308eba`.

- All 17 claim commands passed separately. Logs are under
  `/work/.evidence/api-profile-guard-polish-2-clean/claim-<id>.log`.
- `npm test` passed: 7 Rust unit, 5 CLI integration, and 11 site tests.
- `npm run lint` and `npm run build` passed. `dist/site/` was produced.
- `npm run test:e2e` passed: 30 passed and 20 intentional project-scope skips.
- `cargo build --release` and `cargo package --allow-dirty` passed.
- Built home JavaScript is 3.32 KiB gzip; CSS is 3.89 KiB gzip.
- The factory verifier loaded production in 616 ms with correct title, language,
  H1, main, image alternatives, labelled buttons, and no console errors.
- The final cold live replay passed every route at 1440×900 and 390×844, with
  zero serious or critical axe findings, two distinct demo fingerprints, 44 px
  footer targets, same-origin requests only, no cookies, preserved real-storage
  canaries, and successful offline reload.
- Live routes `/`, `/demo`, `/demo/`, `/?demo=1`, `/privacy/`, and `/terms/`
  returned 200. The designed `/does-not-exist` returned 404. Static metadata
  assets and all linked GitHub destinations returned 200.
- Security headers include CSP, Permissions-Policy, strict referrer policy, HSTS,
  and `nosniff`.
- Live Lighthouse scored 100 performance, 100 accessibility, 100 best practices,
  and 100 SEO. FCP was 0.9 s, LCP 1.1 s, TBT 40 ms, CLS 0, and transfer 61 KiB.

Live evidence is in `/work/.evidence/api-profile-guard-polish-2-live/` and
committed screenshots are in `.factory/polish-2-evidence/`.

## Known gaps and next steps

None for this work order. Registry publication remains a factory-owned release
step; the documented source installation is tested and works now.
